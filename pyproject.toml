[project]
name = "swing-trader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "yfinance>=0.2.0",
    "loguru>=0.7.3",
    "pandas>=3.0.0",
    "pydantic>=2.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
